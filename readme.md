# Copy as Plain Text



Welcome! This is a MV3 extension which allows you to copy text as plain text with a hotkey!

I noticed none of the extensions on the market have this functionality, so this extension is primarily for that.
The extension is currently in alpha, so don't expect too much.